<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <title>Nota</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 40px; background: #f9f9f9; }
        .note-container { background: #fff; padding: 24px; border-radius: 8px; box-shadow: 0 2px 8px #0001; max-width: 600px; margin: auto; }
        .note-title { font-size: 2em; margin-bottom: 12px; }
        .note-content { font-size: 1.2em; margin-bottom: 24px; }
        .note-meta { color: #888; font-size: 0.9em; }
        a { color: #007bff; text-decoration: none; }
        a:hover { text-decoration: underline; }
    </style>
</head>
<body>
    <div class="note-container">
        <div class="note-title"><%= note.title %></div>
        <div class="note-content"><%= note.content %></div>
        <hr>
        <div>
            <a href="/notes">Voltar para minhas notas</a>
            <a href="/notes/<%= note.id %>/edit" style="margin-left: 20px;">Editar Nota</a>
            <form action="/notes/<%= note.id %>/delete?_method=DELETE" method="POST" style="display:inline;">
                <button type="submit" onclick="return confirm('Tem certeza que deseja deletar esta nota?');">Deletar Nota</button>
            </form>
        </div>
    </div>
</body>
</html>